#include "main.h"

/**
 * _strlen - string lengthvoid puts2(char *str);
 * @s: pointer char
 * Return: return length of string
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)

	return (i);
}
